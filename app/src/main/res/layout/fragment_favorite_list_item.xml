<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:padding="5dp">

    <ImageView
        android:id="@+id/favorite_pin"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:src="@drawable/ic_baseline_push_pin_24"
        android:layout_marginRight="5dp"
        android:paddingHorizontal="8dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toLeftOf="@id/favorite_name"/>

    <TextView
        android:id="@+id/favorite_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        tools:text="이름"
        android:textSize="16sp"
        android:textStyle="bold"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toRightOf="@id/favorite_pin"
        app:layout_constraintRight_toLeftOf="@id/favorite_check"
        app:layout_constraintBottom_toTopOf="@id/favorite_zip_code"/>

    <TextView
        android:id="@+id/favorite_zip_code"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="우편번호"
        android:layout_marginVertical="5dp"
        android:layout_marginRight="5dp"
        app:layout_constraintTop_toBottomOf="@id/favorite_name"
        app:layout_constraintLeft_toLeftOf="@id/favorite_name"
        app:layout_constraintRight_toLeftOf="@id/favorite_address"
        app:layout_constraintBottom_toTopOf="@id/favorite_tel"/>

    <TextView
        android:id="@+id/favorite_address"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        tools:text="도로명 주소"
        app:layout_constraintTop_toBottomOf="@id/favorite_name"
        app:layout_constraintLeft_toRightOf="@id/favorite_zip_code"
        app:layout_constraintRight_toRightOf="@id/favorite_name"
        app:layout_constraintBottom_toTopOf="@id/favorite_tel"/>

    <TextView
        android:id="@+id/favorite_tel"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        tools:text="전화번호"
        app:layout_constraintTop_toBottomOf="@id/favorite_zip_code"
        app:layout_constraintLeft_toLeftOf="@id/favorite_zip_code"
        app:layout_constraintRight_toRightOf="@id/favorite_address"
        app:layout_constraintBottom_toBottomOf="parent"/>

    <ImageView
        android:id="@+id/favorite_check"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:src="@drawable/ic_baseline_favorite_24"
        android:layout_marginLeft="5dp"
        android:paddingHorizontal="8dp"
        app:layout_constraintLeft_toRightOf="@id/favorite_name"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintRight_toRightOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>